apiVersion: 1

providers:
  - name: mule
    orgId: 1
    folder: Mule
    folderUid: mule
    type: file
    disableDeletion: false
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
